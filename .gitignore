# Intermediate/working artifacts from the enrichment pipeline — all
# reconstructable by re-running the scripts against the kept source files.
# Not worth long-term version control.

data/blog_posts_raw.json
data/project_tip_scope.csv
data/tagging_input.csv
data/tagged_chunk_*.csv
data/tagged_final_v2.csv
data/uncategorized_candidates.csv
data/uncategorized_recovered.csv
data/uncategorized_tagged_chunk_*.csv
data/pass2_input.csv
data/pass2_chunk_*.csv
data/low_confidence_for_pass2.csv
data/test_batch_pass1.csv

# Airtable data-pull scratch artifacts (field-ID caches, raw pagination
# dumps, a transform helper) -- specific to how the pull was executed, not
# needed going forward. airtable_live_snapshot.json is also excluded: it's
# a diagnostic snapshot of Airtable's current (stale/mis-imported) state,
# not authoritative data -- the site is built from data/searchable_dataset.json.
data/airtable_live_snapshot.json
data/airtable_snapshot.jsonl
data/base_registry.json
data/fieldmap_s1.json
data/transform_append.py

# OS cruft
.DS_Store
